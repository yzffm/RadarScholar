# =============================================================================
# RadarScholar Backend — Python Dependencies
# =============================================================================

# --- Web Framework ---
fastapi>=0.115.0
uvicorn>=0.34.0

# --- Data Validation ---
pydantic>=2.0
pydantic-settings>=2.0

# --- Database ---
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9

# --- Authentication ---
PyJWT>=2.8

# --- Environment ---
python-dotenv>=1.0

# --- HTTP Client (for testing and future crawler) ---
httpx>=0.27

# --- Testing ---
pytest>=8.0

# --- Code Quality ---
# NOTE: Install ruff separately via `pip install ruff` or system package manager.
# The [standard] extras for uvicorn (watchfiles) and ruff require Rust toolchain
# to build from source on some platforms. If pre-built wheels are unavailable,
# install ruff globally: `pip install ruff`
